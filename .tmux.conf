# Dracula Colours
# background_color '#282a36'
# current_line_color '#44475a'
# foreground_color '#f8f8f2'
# comment_color '#6272a4'
# cyan '#8be9fd'
# green '#50fa7b'
# orange '#ffb86c'
# pink '#ff79c6'
# purple '#bd93f9'
# red '#ff5555'
# yellow '#f1fa8c'

# support 256 color term
set -g default-terminal "screen-256color"

# Set CTRL-Space as prefix
set -g prefix C-Space
unbind C-b
bind C-Space send-prefix

# More responsive
set -s escape-time 1

# window and panes numbers start at 1
set -g base-index 1
setw -g pane-base-index 1

# Mouse
setw -g mouse on

# Bindings

# Reload conf
bind r source-file ~/.tmux.conf \; display "Reloaded Conf!"

bind c new-window -c "#{pane_current_path}"

# Better split windows
bind | split-window -h
bind - split-window -v

# Move between panes the Vim way
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Move between windows
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Resize Panes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

bind k kill-session
bind q kill-server

## Restore Neovim sessions
set -g @resurrect-strategy-nvim 'session'
## Restore Panes
set -g @resurrect-capture-pane-contents 'on'

setw -g automatic-rename off
set -g @continuum-boot 'on'
set -g @continuum-restore 'on'
set -g @continuum-boot-options 'iterm,fullscreen'

# source-file ~/Source/tmuxline.conf

# Theme
# Status
# set -g status "on"
# set -g status-interval 1
# set -g status-style bg="#44475a",fg="#bd93f9"
# set -g status-justify "centre"
# set -g status-attr "none"
# set -g status-left-length "100"
# set -g status-left-attr "none"
# set -g status-right-length "100"
# set -g status-right-attr "none"
# set -g status-left '#[bg=#f8f8f2]#[fg=#282a36]#{?client_prefix,#[bg=#ff79c6],} ☺ '
# set -ga status-left '#[bg=#44475a]#[fg=#ff79c6] #{?window_zoomed_flag, ↕  ,   }'
# set -g status-left "#[fg=black,bg=green] #S #[fg=colour65,bg=colour235,nobold,nounderscore,noitalics]#[fg=colour250,bg=colour235] #F #[fg=colour235,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour250,bg=colour237] #W #[fg=colour237,bg=colour237,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=colour65,bg=colour237]#{prefix_highlight} #{battery_status_bg} Batt: #{battery_icon} #{battery_percentage} #{battery_remain} #[fg=colour237,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour250,bg=colour237] %a #[fg=colour235,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour250,bg=colour235] %b %d %R #[fg=colour65,bg=colour235,nobold,nounderscore,noitalics]#[fg=black,bg=green] #H "
# set -g status-right '#[fg=#44475a,bg=#50fa7b] #{battery_icon} #{battery_percentage} #{battery_remain} '
# set -ga status-right '#[fg=#44475a,bg=#ff79c6] #(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") '
# set -ga status-right '#[fg=#f8f8f2,bg=#bd93f9] %a %H:%M:%S #[fg=#44475a]%Y-%m-%d '

# Pane
# set -g pane-border-style fg="#6272a4"
# set -g pane-active-border-style fg="#ff79c6"

# message
# set -g message-style bg="#44475a",fg="#8be9fd"
# set -g message-command-fg "colour250"
# set -g message-command-bg "colour235"

# window
# set-window-option -g window-status-style fg='#bd93f9',bg=default
# set-window-option -g window-status-current-style fg='#ff79c6',bg='#282a36'

# setw -g window-status-fg "colour250"
# setw -g window-status-bg "colour237"
# setw -g window-status-attr "none"
# setw -g window-status-activity-fg "colour65"
# setw -g window-status-activity-bg "colour237"
# setw -g window-status-activity-attr "none"
# setw -g window-status-separator ""
# setw -g window-status-format "#[fg=colour237,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour65,bg=colour237] #I #W #[fg=colour237,bg=blue,nobold,nounderscore,noitalics]"
# setw -g window-status-current-format "#[fg=colour237,bg=colour65,nobold,nounderscore,noitalics]#[fg=black,bg=green] #I #W #[fg=colour65,bg=colour237,nobold,nounderscore,noitalics]"

# set -g window-status-format "#[fg=#f8f8f2]#[bg=#44475a] #I #W "
# set -g window-status-current-format "#[fg=#44475a]#[bg=#bd93f9] #I #W "

set -g @dracula-plugins "battery git time"
set -g @dracula-show-powerline true

# for left
set -g @dracula-show-left-sep 
# for right symbol (can set any symbol you like as seperator)
set -g @dracula-show-right-sep 

set -g @dracula-show-flags true

# the default is 5, it can accept any number
set -g @dracula-refresh-rate 5

# it can accept `session`, `smiley`, `window`, or any character.
set -g @dracula-show-left-icon session

# default is 1, it can accept any number and 0 disables padding.
set -g @dracula-left-icon-padding 1

# time
set -g @dracula-show-timezone false
set -g @dracula-day-month true
set -g @dracula-military-time true

# weather
set -g @dracula-show-fahrenheit false

# battery
set -g @dracula-battery-label 

# git
set -g @dracula-git-disable-status true
# default is ✓. Avoid using non unicode characters that bash uses like $, * and ! 
set -g @dracula-git-show-current-symbol 
# default is unicode !. Avoid bash special characters 
set -g @dracula-git-show-diff-symbol 
# default is unicode no message
set -g @dracula-git-no-repo-message 

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'dracula/tmux'

# Initializes TMUX plugin manager.
# Keep this line at the very bottom of tmux.conf.
run -b '~/.tmux/plugins/tpm/tpm'

